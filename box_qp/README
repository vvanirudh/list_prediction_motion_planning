First time compiling

1. create a folder called build in the same directory as this README file (dont commit this folder ever to git)
2. cd build
3. cmake ..
4. make

Subsequent builds
1. cd build
2. make

All binaries exists in build. To run a binary (such as the example minqp_d_bc1) type
./minqp_d_bc1
